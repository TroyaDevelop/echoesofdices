# EOTD20

Минималистичный сайт мира EOTD20: публично доступны только «Новости» и «Заклинания». Добавление/удаление контента — только через админку.

## Описание

Публичная часть — read-only.

Админка — управление новостями и заклинаниями (создание/удаление).

## Функциональность

### Публичная часть
- 📰 Просмотр новостей
- ✨ Просмотр списка заклинаний (поиск)
- 📱 Адаптивный дизайн
- ⚡ Быстрая навигация

### Административная панель
- 🔐 Система аутентификации (только вход)
- ✏️ Создание/удаление новостей
- ✨ Создание/удаление заклинаний
- 🔑 Генерация ключей регистрации и управление ролями пользователей

## Технологический стек

### Frontend
- **React 19** - SPA (React Router)
- **esbuild** - сборка/дев-сервер
- **Tailwind CSS 4 (через PostCSS)** - стили

### Backend
- **Node.js** - серверная среда выполнения
- **Express.js** - веб-фреймворк
- **JWT** - аутентификация
- **bcryptjs** - хеширование паролей
- **MariaDB** - база данных (в docker-compose)

## Установка и запуск

### Предварительные требования
- Node.js 18+
- npm или yarn
- Docker + Docker Compose (для запуска через `make echoes-*`)

### Шаги установки

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd eotd20site
```

2. **Установка зависимостей**
```bash
npm install
npm --prefix frontend install
npm --prefix backend install
```

3. **Настройка окружения (опционально)**

Frontend использует переменную `API_URL` (подхватывается в сборке/esbuild).

```env
# пример
API_URL=http://localhost:5017/api
```

4. **Запуск проекта (локально, без Docker)**
```bash
npm run dev
```

Запустит одновременно:
- Frontend на http://localhost:3017 (или порт из `FRONTEND_PORT`)
- Backend API на http://localhost:5017

### Запуск через Docker (рекомендуется)

```bash
make echoes-build
make echoes-up
make echoes-log
make echoes-down
```

### Деплой на VPS (по IP, порт 80)

На сервере понадобится Docker + Docker Compose.

Запуск:

```bash
docker compose -f docker-compose.prod.yml up -d --build
```

После этого сайт будет доступен на `http://<IP>/`, а API проксируется как `http://<IP>/api`.

Редакторы создаются из админки (после входа админом), без ключей регистрации.

## Структура проекта

```
eotd20site/
├── frontend/
│   ├── src/                   # SPA страницы/компоненты
│   └── Dockerfile             # nginx runtime
├── backend/
│   ├── database/              # Схемы БД
│   └── server.js              # Основной сервер
├── public/                     # общие статические файлы (карта и т.п.)
└── package.json
```

## API

### Публичные эндпоинты
- `GET /api/news` - список опубликованных новостей
- `GET /api/spells` - список заклинаний

### Административные эндпоинты
- `POST /api/auth/login` - вход в систему

Регистрация пользователей:
- `POST /api/auth/register` - регистрация по ключу (ключ выдаёт редактор)

Пользователи/ключи (редактор):
- `GET /api/admin/users` - список пользователей
- `PATCH /api/admin/users/:id/role` - выдать/забрать роль editor
- `POST /api/admin/registration-keys` - создать ключ регистрации
- `GET /api/admin/registration-keys` - список ключей регистрации

Новости:
- `GET /api/news/admin` - список всех новостей (редактор)
- `POST /api/news` - создание новости (редактор)
- `DELETE /api/news/:id` - удаление новости (редактор)

Заклинания:
- `GET /api/spells/admin` - список всех заклинаний (редактор)
- `POST /api/spells` - создание заклинания (редактор)
- `DELETE /api/spells/:id` - удаление заклинания (редактор)

## Разработка

### Добавление новых страниц
Создайте новые файлы в `frontend/src/pages` и добавьте роут в `frontend/src/App.jsx`.

### Добавление API эндпоинтов
Добавьте новые роуты в `backend/routes/` и подключите в `server.js`.

### Стилизация
Проект использует Tailwind CSS. Классы можно добавлять напрямую в JSX.

## Лицензия

MIT License

## Автор

Miyuki - создано для эпических RPG приключений ⚔️